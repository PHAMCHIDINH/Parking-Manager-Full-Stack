spring.application.name=parkingmanager

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/parking_manager?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=123456

# Hibernate & JPA Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh

# Jackson JSON timezone (mainly affects Date/Zoned types; LocalDateTime remains offset-less)
spring.jackson.time-zone=Asia/Ho_Chi_Minh

# Connection Pooling (HikariCP Optimized Settings)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Disable Console Banner
spring.main.banner-mode=off

file.upload-dir=/home/zakaria/uploads

